// GENERATED by scripts/sync-plugin-core.mjs from plugins/core — edit there, then re-run the sync.
import { connectForProject } from "../broker-client.js";
import { readState, deleteState, sweepStale, isUsed, buildEpisodeBatch } from "../recorder.js";
import { recordingDisabled } from "../hook-io.js";
/** Flush the session's episode. `assistantText === null` means "transcript
 *  unknown": retrievals are recorded but usage is NOT judged. */
export async function flushEpisode({ dataDir, env, projectDir, sessionId, assistantText, reason = "", connect = connectForProject, now = Date.now }) {
  if (!dataDir) return "disabled";
  sweepStale(dataDir);
  if (recordingDisabled(env ?? {}) || !sessionId) return "disabled";
  const state = readState(dataDir, sessionId);
  if (!state || !state.retrievals.length) { deleteState(dataDir, sessionId); return "no-state"; }
  const judged = typeof assistantText === "string";
  const used = new Map();
  if (judged) {
    state.retrievals.forEach((r, i) => {
      const ids = new Set();
      for (const m of r.returned) { const c = state.contents[m.id]; if (c && assistantText && isUsed(c, assistantText)) ids.add(m.id); }
      if (ids.size) used.set(i, ids);
    });
  }
  const cmds = buildEpisodeBatch({ state, outcome: "success", failure: "", endedAt: now(), used, reason: typeof reason === "string" ? reason : "", usageJudged: judged });
  const client = await connect({ projectDir, dataDir });
  if (!client) return "no-daemon"; // state file left for a later sweep
  try { await client.call("submit_batch", { commands: cmds }, 5000); deleteState(dataDir, sessionId); return "flushed"; }
  catch { return "failed"; }
  finally { client.close(); }
}
