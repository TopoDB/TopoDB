[workspace]
resolver = "2"
members = [
    "crates/topodb",
    "crates/topodb-json",
    "crates/topodb-mcp",
    "crates/topodb-cli",
]

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
