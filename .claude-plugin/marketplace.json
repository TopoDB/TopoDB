{
  "name": "topodb",
  "owner": { "name": "TopoDB" },
  "description": "Persistent agent memory for Claude Code: a temporal property graph, scoped per project with a shared layer.",
  "plugins": [
    {
      "name": "topodb",
      "source": "./plugins/claude-code",
      "description": "Persistent agent memory: a temporal property graph, scoped per project with a shared layer."
    },
    {
      "name": "sgh",
      "source": "./plugins/sgh",
      "description": "Plan and run agent work as a validated graph with a computable worst-case bound, instead of an open-ended loop."
    }
  ]
}
