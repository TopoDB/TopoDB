# topodb-cli

A direct-embedded, script-friendly command-line interface over a [TopoDB](https://crates.io/crates/topodb)
agent-memory database file. JSON in, JSON out, predictable exit codes — no server process.

Status: **scaffold** — only `info` is implemented so far. Full command reference lands in a
later task.
